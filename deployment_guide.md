# 🏁 دليل تشغيل ونشر متجر أبو تريكة الرياضي الذكي
مرحباً بك يا مشرفنا العزيز! هذا هو الدليل الشامل والمفصل خطوة بخطوة باللغة العربية البسيطة لتوضيح كيفية تشغيل المتجر محلياً على جهازك، وكيفية ربط أجزائه، وكيفية رفعه على الإنترنت مجاناً للحصول على رابط مباشر (Link) ترسله لعملائك لتبدأ في بيع المنتجات واستقبال طلبات الحجز الفوري على الواتساب.

---

## 📁 1. أين توجد ملفات المشروع وكيف تترتب؟
يتكون مشروعك من جزأين رئيسيين مدمجين معاً بشكل عبقري (واجهة أمامية وخلفية وقاعدة بيانات):

1. **الفرونت إند (Front-End - واجهة المتجر للعملاء):**
   * توجد بالكامل داخل المجلد المسمى `public` في الجانب الأيسر.
   * **`public/index.html`:** هو الهيكل والتصميم الأساسي للمتجر، وفيه النوافذ المنبثقة التفاعلية (نافذة الحجز، استوديو الـ 3D، ونافذة تأصيل المنتجات المكبرة).
   * **`public/styles.css`:** ملف التنسيق والألوان والمؤثرات البصرية المتوهجة (ألوان النيون الرياضية الفاخرة وتأثير تكبير الأنسجة المجهري).
   * **`public/app.js`:** العقل المدبر للمتجر، الذي يتعرف تلقائياً على حالة السيرفر، ويقوم بتوليد شهادات التأصيل الرقمية، ويحسب زوايا التكبير الميكروسكوبي، ويوجه رسائل الحجز للواتساب مباشرة.

2. **الباك إند (Back-End - الخادم الخلفي للموقع):**
   * **`server.js`:** السيرفر المبني بلغة Node.js وإطار Express. وهو المسؤول عن توفير روابط الـ API لتسجيل الدخول الآمن، رفع صور المنتجات، استعادة كلمة السر، وحفظ المنتجات والطلبات.
   * **مجلد `uploads/`:** مجلد يتم إنشاؤه تلقائياً لحفظ الصور التي يرفعها المسؤول عند إضافة منتج جديد.

3. **قاعدة البيانات (Database):**
   * **`database.sqlite`:** ملف قاعدة البيانات الذاتي النشأة. بمجرد تشغيل السيرفر لأول مرة، يتم إنشاؤه تلقائياً ويحتوي على جداول المنتجات، الطلبات، وبيانات المسؤول ورقم الواتساب الخاص به.

---

## 🔄 2. كيف يرتبط المتجر ببعضه؟ (الوضع الهجين الذكي)
المشروع مبني بنظام **الهجين الذكي (Hybrid Engine)** لحماية متجرك من التوقف:
* **إذا كان السيرفر متصلاً (Online):** يقوم المتجر بحفظ وقراءة البيانات من قاعدة بيانات SQLite في السيرفر بشكل طبيعي وسريع جداً.
* **إذا كان السيرفر غير متصل (Offline / الوضع المحلي):** لا يقلق المتجر ولا يتوقف! ينتقل فوراً وبسلاسة تامة للعمل بالوضع المحلي الآمن (`localStorage`) داخل متصفحك، مما يحفظ منتجاتك وإعداداتك وطلباتك داخل ذاكرة المتصفح بحيث لا تفقد أي شيء أبداً.

---

## 💻 3. تشغيل المتجر محلياً على جهازك خطوة بخطوة
لتجربة وتشغيل المتجر على جهاز الكمبيوتر الخاص بك، اتبع الخطوات البسيطة التالية:

1. **تحميل برنامج Node.js:**
   * اذهب للموقع الرسمي [nodejs.org](https://nodejs.org) وقم بتحميل وتثبيت نسخة LTS المستقرة.

2. **فتح موجه الأوامر (Terminal / Command Prompt):**
   * افتح برنامج Terminal في المجلد الرئيسي للمشروع (aboutrika-store).

3. **تثبيت المكونات الأساسية (Dependencies):**
   * اكتب الأمر التالي واضغط Enter لتثبيت جميع الحزم اللازمة تلقائياً:
     ```bash
     npm install
     ```

4. **تشغيل سيرفر المتجر:**
   * اكتب الأمر التالي لتشغيل السيرفر:
     ```bash
     npm start
     ```
   * ستظهر لك رسالة ترحيبية تخبرك أن السيرفر يعمل الآن بنجاح على الرابط التالي:
     `http://localhost:3000`

5. **فتح المتجر:**
   * افتح متصفح الإنترنت واكتب `http://localhost:3000` لمشاهدة المتجر في قمة تألقه وجماله.

---

## 🚀 4. كيفية رفع المتجر ونشره مجاناً (للحصول على رابط مباشر للعملاء)
لتحصل على رابط مباشر يبدأ بـ `https://` ويركب عليه موقعك ليدخله أي شخص من أي هاتف في العالم، ننصحك باستخدام منصة **Render** المجانية لأنها تدعم قواعد البيانات وتخزين الصور وتوفر حماية كاملة.

### الخطوة أ: إعداد الكود على GitHub (مستودع الأكواد)
1. قم بإنشاء حساب مجاني على موقع [GitHub.com](https://github.com).
2. قم بإنشاء مستودع جديد (Repository) وسمه `aboutrika-store`.
3. ارفع جميع ملفات مشروعك (بما في ذلك مجلد `public` وملف `server.js` و `package.json`) إلى المستودع. 
   *(💡 تنبيه: لا ترفع ملف `database.sqlite` ومجلد `node_modules` لأن السيرفر سينشئهم تلقائياً).*

### الخطوة ب: الرفع والنشر على منصة Render
1. توجه إلى موقع [Render.com](https://render.com) وأنشئ حساباً مجانياً عبر ربطه بحساب GitHub الخاص بك.
2. في لوحة التحكم، اضغط على زر **New +** ثم اختر **Web Service**.
3. اختر مستودع مشروعك `aboutrika-store` الذي رفعته على GitHub واضغط **Connect**.
4. قم بملء البيانات كالتالي:
   * **Name:** `aboutrika-store`
   * **Region:** اختر أقرب منطقة لك (مثال: Frankfurt).
   * **Branch:** `main` (أو الفرع الأساسي المرفوع عليه الكود).
   * **Runtime:** `Node`
   * **Build Command:**
     ```bash
     npm install
     ```
   * **Start Command:**
     ```bash
     npm start
     ```
   * **Instance Type:** اختر الباقة المجانية (**Free**).

5. **تفعيل حفظ قاعدة البيانات بشكل دائم (Persistent Disk):**
   * بما أن Render المجاني يمسح الملفات المؤقتة عند إعادة التشغيل، لمنع حذف صور المنتجات وقاعدة البيانات:
   * انزل لأسفل واضغط على قسم **Advanced** ثم اضغط على **Add Disk** (أو تفعيل قرص دائم).
   * **Mount Path:** اكتب `/opt/render/project/src/uploads` لحفظ صور المنتجات دائماً.
   * **Size:** 1 GB (مجاني تماماً ويكفي آلاف المنتجات).
   * أضف متغير بيئي (Environment Variable) في قسم Variables كالتالي:
     * الاسم (Key): `DATABASE_PATH`
     * القيمة (Value): `/opt/render/project/src/database.sqlite` (لحفظ قاعدة البيانات على القرص الدائم).

6. اضغط على **Create Web Service**.
7. ستبدأ المنصة ببناء وتجهيز موقعك خلال دقيقتين، وسوف يظهر لك رابط في الأعلى باللون الأخضر مثل:
   `https://aboutrika-store.onrender.com`

---

## 🌟 5. كيف يتم إيصال المتجر للعملاء واستقبال الأرباح؟
1. بمجرد انتهاء الرفع، انسخ الرابط المباشر للموقع.
2. انشره على صفحات الفيسبوك، الإنستغرام، أو عبر رسائل الواتساب والجروبات.
3. **تجربة المشتري:** 
   * يدخل العميل على رابط متجرك، فينبهر بالتصميم المستقبلي الجذاب.
   * يضغط على أي منتج ليعرض شهادة الأصالة، بلد المنشأ، والخامات بالتفصيل.
   * يمرر إصبعه أو الماوس لرؤية النسيج بوضوح تام عبر **عدسة التكبير البصري**.
   * يختار المقاس واللون المفضل ويضغط **تأكيد الحجز الفوري بالواتساب**.
   * يفتح المتجر تطبيق الواتساب للعميل تلقائياً وفيه رسالة منسقة وجاهزة ببياناته واسمه وعنوانه، ليرسلها إليك مباشرة لتأكيد الطلب والتوصيل الفوري!

🎉 **متجرك الآن جاهز للعمل والنشر وبدء النجاح الرياضي!** 🎉
